# TODO — index.html

- [ ] Portrait photo — hero card still placeholder (initials). OG share image also uses a generated brand card, not a real photo.
- [ ] Case study links — Nanameue card still has no href (Hanabi Finder, Liquid Lite, Anytimes, Godfisk now do).
- [ ] Social proof / testimonials — no client quotes yet (consulting has no clients to date per business docs); could add engineering-credibility quotes from former colleagues in the meantime.
- [ ] ai-coding-standards.html (new lead-magnet page) needs a Kit (ConvertKit) account + form: create one free, get the Form ID and public API key, replace `YOUR_CONVERTKIT_FORM_ID` / `YOUR_CONVERTKIT_API_KEY` in the page's script. Signups won't work until this is done.
- [ ] The actual AI Coding Standards / Prompt Pack content doesn't exist yet — page promises it but nothing is written. Needs to exist before real signups happen, since Kit won't auto-send a file that isn't there.
